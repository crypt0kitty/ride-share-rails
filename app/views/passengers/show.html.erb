<!DOCTYPE html>
<html>
<head>
<title>RideShareRails</title>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
</head>
<body>
<div class="header">
  <h1> Passenger: <%= @passenger.name %></h1>
</div>
<div class="nav">
  <%= link_to "Home", homepages_path %>
  <%= link_to "Passengers", passengers_path%>
  <%= link_to "Drivers", drivers_path%>
  </div>
<main class="main-container">
<h3 class= "passenger-title">Additional Passenger Information</h3>
<ul id = "passenger-info-trip-list">
  <li>Passenger ID: <%= @passenger.id %></li>
  <li>Total amount billed: $<%= @passenger.passenger_cost %></li>
    <li>Your phone number is: <%= @passenger.phone_num %></li>
  </h4>
</ul>
<table>
  <thead>
  <tr>
    <th>Trip ID</th>
    <th>Driver</th>
    <th>Passenger</th>
    <th>Trip Cost</th>
    <th>Trip Rating</th>
  </tr>
  </thead>
  <tbody>
  <% @passenger.trips.each do |trip|%>
  <tr>
    <td><%= trip.id %>, <%= link_to "Trip Details", trip_path(trip.id) %></td>
    <td><%= trip.driver.name %></td>
    <td><%= trip.passenger.name %></td>
    <td><%= number_to_currency(Trip.format_money(trip.cost)) %></td>
    <td><%= trip.rating %></td>
  </tr>
  <% end %>
  </tbody>
</table>
<p class="edit-trip-text"><%= link_to 'Add new trip', passenger_trips_path(@passenger), class: "a-primary", method: :post %></p>
</main>
<footer class="footer">
<p id = "footer-text">© Sandy and Rachel</p>
</footer>
</body>
</html>



